#### Virtual Object: `@task`

Description: the current task plan list (markdown).

###### Task Content

{{.}}

#### Require

**KEEP the exact format: `- [X] taskName: taskDetails`, 2-space indent per level.**
**Only `-` bullet is allowed. Status: `[ ]` waiting, `[-]` doing, `[X]` done.**
