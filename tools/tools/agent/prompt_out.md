### Tool: `deactivate_agent`

#### Description:

Terminates the current subagent session and returns the final output or error message to the primary agent. This tool must be called to conclude the subagent's lifecycle.
